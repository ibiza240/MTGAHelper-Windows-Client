<UserControl x:Class="MTGAHelper.Tracker.WPF.Views.UserControls.Playing"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:MTGAHelper.Tracker.WPF.Views"
        xmlns:helpers="clr-namespace:MTGAHelper.Tracker.WPF.Views.Helpers"
        xmlns:uc="clr-namespace:MTGAHelper.Tracker.WPF.Views.UserControls"
        mc:Ignorable="d" 
        d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <helpers:BoolToStringConverter x:Key="HasPriorityConverter" TrueValue="Bold" FalseValue="Regular" />
    </UserControl.Resources>
    <UserControl.Style>
        <Style TargetType="{x:Type uc:Playing}">
            <Setter Property="Visibility" Value="Hidden" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding MainWindowContext}" Value="Playing">
                    <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <!--<Label Grid.Row="0"  FontSize="20" FontWeight="Bold" Padding="5,0,5,0">
            <TextBlock>Match in progress</TextBlock>
        </Label>-->
        
        <TabControl Grid.Row="2" TabStripPlacement="Left" Margin="0" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="{x:Type TabItem}">
                    <Setter Property="HeaderTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <ContentPresenter>
                                    <ContentPresenter.LayoutTransform>
                                        <RotateTransform Angle="270" />
                                    </ContentPresenter.LayoutTransform>
                                    <ContentPresenter.Content>
                                        <TextBlock Text="{Binding}" Foreground="#f26d21" />
                                    </ContentPresenter.Content>
                                    <!--<ContentPresenter.Style>
                                        <Style TargetType="ContentPresenter">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TabItem}}" Value="True">
                                                    <Setter Property="TextBlock.Foreground" Value="Black"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TabItem}}" Value="False">
                                                    <Setter Property="TextBlock.Foreground" Value="White"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ContentPresenter.Style>-->
                                </ContentPresenter>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Padding" Value="1 10" />
                    <Setter Property="Background" Value="Transparent" />
                </Style>
            </TabControl.Resources>
            <TabItem Header="Library">
                <TabItem.Content>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Label Grid.Row="0" FontSize="16" FontWeight="Bold" Padding="5,0,0,0">
                            <TextBlock Text="{Binding InMatchState.LibraryCardsCount, StringFormat=Your library ({0})}" />
                        </Label>
                        <Label Grid.Row="1" Margin="0" Padding="5,0,0,5">
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="Probability of a land ({0}) on next draw: {1:p1}">
                                        <Binding Path="InMatchState.LibraryLandsCount" />
                                        <Binding Path="InMatchState.Stats.DrawLandPct.Value" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Label>
                        <uc:SimpleCardsList Grid.Row="2" x:Name="CardsLibrary" />
                    </Grid>
                </TabItem.Content>
            </TabItem>

            <TabItem Header="Opponent cards">
                <TabItem.Content>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Label Grid.Row="0" FontSize="16" FontWeight="Bold">
                            <TextBlock>Opponent cards seen</TextBlock>
                        </Label>
                        <uc:SimpleCardsList Grid.Row="1" x:Name="CardsOpponent" DataContext="{Binding InMatchState.OpponentCardsSeen}" />
                    </Grid>
                </TabItem.Content>
            </TabItem>

            <!--<TabItem Header="Stats">
                <TabItem.Content>
                    <Label FontSize="16" FontWeight="Bold">
                        <TextBlock>Stats</TextBlock>
                    </Label>
                </TabItem.Content>
            </TabItem>

            <TabItem Header="Full deck">
                <TabItem.Content>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Label Grid.Row="0" FontSize="16" FontWeight="Bold">
                            <TextBlock>Full deck with sideboard</TextBlock>
                        </Label>
                        <uc:SimpleCardsList Grid.Row="1" x:Name="FullDeck" DataContext="{Binding InMatchState.FullDeck}" />
                    </Grid>
                </TabItem.Content>
            </TabItem>-->
        </TabControl>
        
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0" FontSize="14" FontWeight="Bold" Margin="0" Padding="0">
                <TextBlock Text="Priority" />
            </Label>
            <Label Grid.Row="0" Grid.Column="1" Margin="0" Padding="10,0,0,0" VerticalAlignment="Bottom"
                FontWeight="{Binding InMatchState.TimerMe.HasPriority, Converter={StaticResource HasPriorityConverter}}">
                <TextBlock Text="{Binding InMatchState.TimerMe.TimePlayed, StringFormat=You: {0}}" />
            </Label>
            <Label Grid.Row="0" Grid.Column="2" Margin="0" Padding="10,0,0,0" VerticalAlignment="Bottom"
                FontWeight="{Binding InMatchState.TimerOpponent.HasPriority, Converter={StaticResource HasPriorityConverter}}">
                <TextBlock Text="{Binding InMatchState.TimerOpponent.TimePlayed, StringFormat=Opponent: {0}}" />
            </Label>
        </Grid>
    </Grid>
</UserControl>
